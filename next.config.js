/** @type {import('next').NextConfig} */
const nextConfig = {
  // Define your configuration options here
  env: {
    MY_CUSTOM_ENV_VARIABLE: "my-custom-value",
  },
  // Add any other Next.js configurations you need
};

module.exports = nextConfig;
